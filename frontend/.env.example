# Backend API base URL.
# In production: set to your backend host (no trailing slash).
# In development: leave unset — Vite's dev proxy forwards /api to localhost:8000.
VITE_API_BASE_URL=https://api.your-backend-host.com
